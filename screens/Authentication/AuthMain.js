import {
  StyleSheet,
  Text,
  View,
  Image,
  FlatList,
  TouchableOpacity,
  TouchableWithoutFeedback,
} from 'react-native'
import React from 'react'
import TextButton from '../../components'
import { SIZES, images, icons, FONT, COLORS } from '../../constants'

const AuthMain = () => {
  return (
    <View>
      <Text>AuthMain</Text>
    </View>
  )
}

export default AuthMain

const styles = StyleSheet.create({})
